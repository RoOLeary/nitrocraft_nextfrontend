name: Bump version
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: '0'
    - name: Bump version and push tag
      uses: anothrNick/github-tag-action@1.40.0
      env:
        GITHUB_TOKEN: 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
        DEFAULT_BUMP: major
        REPO_OWNER: RoOLeary
        WITH_V: true
        INITIAL_VERSION: v1.0.0
        RELEASE_BRANCHES: main